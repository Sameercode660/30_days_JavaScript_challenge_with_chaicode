// use the map method to create a new array where each element is doubled and log the result.

const array = [1,2,3,4,5]

const doubledArray = array.map(e => e * e)

console.log(array)
console.log(doubledArray)