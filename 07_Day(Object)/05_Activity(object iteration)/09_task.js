// Use Object.key and Object.values method to log all the key and values of the book object

const book = {
    title: "The sword",
    author: "Martin j. Moses",
    year: 1990
}

const keys = Object.keys(book)
const values = Object.values(book)

console.log(keys)
console.log(values)